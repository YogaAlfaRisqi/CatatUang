enum UserRole {
  USER
  ADMIN
  SUPER_ADMIN

  @@schema("user")
}

enum AuthProvider {
  EMAIL
  GOOGLE
  FACEBOOK
  APPLE

  @@schema("user")
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENED
  DELETED

  @@schema("user")
}

enum TransactionType {
  INCOME
  EXPENSE
  TRANSFER

  @@schema("transaction")
}

enum CategoryType {
  INCOME
  EXPENSE
  BOTH

  @@schema("category")
}

enum RecurrenceFrequency {
  DAILY
  WEEKLY
  MONTHLY
  YEARLY

  @@schema("transaction")
}

enum GoalStatus {
  ACTIVE
  COMPLETED
  CANCELLED
  PAUSED

  @@schema("goal")
}

enum NotificationType {
  BUDGET_WARNING
  BUDGET_EXCEEDED
  GOAL_MILESTONE
  RECURRING_REMINDER
  REPORT_READY

  @@schema("notification")
}

enum Currency {
  IDR
  USD
  EUR
  SGD
  MYR

  @@schema("user")
}
